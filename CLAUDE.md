# AINEXTEDU

AI 기반 교육 플랫폼 프로젝트. 동영상 콘텐츠 분석, 자막 처리, 프레임 추출 등을 통해 학습 경험을 제공한다.

---

## 개발 워크플로우 (Superpowers 방법론)

`obra/superpowers` 기반의 구조화된 개발 방법론을 따른다.

### 1단계: 브레인스토밍 (구현 전 필수)

복잡도와 무관하게 구현 전에 반드시 설계 대화를 먼저 한다. 단순해 보이는 작업도 검토하지 않은 가정이 가장 큰 낭비를 만든다.

- 기존 프로젝트 컨텍스트를 먼저 탐색한다
- 명확화 질문을 하나씩 순서대로 묻는다
- 2–3가지 접근법과 트레이드오프를 제시한다
- 설계 내용을 스펙 파일로 문서화하고 사람이 검토한다
- 브레인스토밍이 끝나면 곧바로 계획 작성으로 넘어간다

### 2단계: 계획 작성

코딩 전에 실행 가능한 계획을 작성한다. 계획에는 반드시 포함한다:

- 기능명과 한 문장 목표
- 아키텍처 요약 및 기술 스택
- 수정·생성할 파일 목록 (각 파일의 단일 책임 명시)
- 각 태스크: 정확한 파일 경로, 테스트 → 구현 → 커밋 순서
- 플레이스홀더 없이 실제 구현 세부 사항 포함

각 태스크는 독립적으로 테스트 가능한 단위 (약 2–5분 작업)로 쪼갠다.

### 3단계: TDD로 구현

**실패하는 테스트 없이 프로덕션 코드를 작성하지 않는다.**

RED → GREEN → REFACTOR 사이클:

1. **RED**: 원하는 동작을 증명하는 최소 실패 테스트 작성
2. **GREEN**: 그 테스트를 통과시키는 가장 단순한 코드 작성
3. **REFACTOR**: 테스트가 통과된 상태에서 정리

TDD 예외: 일회성 프로토타입, 생성된 코드, 설정 파일 (명시적 승인 필요).

TDD 이탈 신호: 테스트보다 코드를 먼저 작성, 테스트가 즉시 통과, "이번 한 번만" 합리화.

### 4단계: 체계적 디버깅

**증상 수정은 실패다. 반드시 근본 원인을 찾는다.**

4단계 디버깅 프로세스:

1. **근본 원인 조사**: 에러 메시지 정독, 문제 재현, 최근 변경 사항 확인, 호출 스택 역추적
2. **패턴 분석**: 코드베이스에서 동작하는 예시 찾기, 차이점 비교
3. **가설 검증**: 구체적 가설 수립 → 최소 변경으로 검증 (한 번에 하나)
4. **구현**: 실패 테스트 먼저 → 근본 원인 수정 → 전체 테스트 통과 확인

3번 수정 시도 후에도 실패하면: 더 많은 패치가 아니라 아키텍처를 의심한다.

### 5단계: 완료 전 검증

**신선한 검증 증거 없이 완료를 선언하지 않는다.**

검증 게이트:

1. 주장을 증명하는 명령어를 식별한다
2. 명령어를 새로 실행한다 (캐시된 결과 불인정)
3. 전체 출력과 종료 코드를 읽는다
4. 출력이 주장을 확인하는지 검증한다
5. 그 후에만 증거와 함께 완료를 선언한다

"should", "probably", "seems" 같은 표현은 검증이 안 된 신호다.

---

## 비디오 분석 스킬 (`/watch`)

`bradautomates/claude-video` 스킬을 기반으로 한 동영상 분석 파이프라인.

### 설치

```bash
# Claude Code
/plugin marketplace add bradautomates/claude-video

# 수동 설치
git clone https://github.com/bradautomates/claude-video.git ~/.claude/skills/watch
```

### 동작 방식

1. `yt-dlp`로 URL 또는 로컬 파일 다운로드
2. `ffmpeg`으로 자동 스케일 프레임 추출 (JPEG)
3. 네이티브 캡션 또는 Whisper API로 타임스탬프 전사
4. 프레임 + 전사본을 Claude에 전달해 질문 응답

### 지원 소스

- YouTube, TikTok, Vimeo, Instagram URL
- 로컬 파일: MP4, MOV, MKV, WebM

### 프레임 추출 전략

| 영상 길이 | 추출 프레임 수 | 밀도 |
|-----------|---------------|------|
| ≤ 30초    | ~30 프레임 (~1-2 fps) | 조밀 |
| 30초–1분  | ~40 프레임 | 조밀 |
| 1–3분     | ~60 프레임 | 보통 |
| 3–10분    | ~80 프레임 | 희소 |
| > 10분    | 100 프레임 (희소 스캔 — 정확도 주의) | 희소 |

10분 초과 영상은 `--start`/`--end` 구간 지정을 권장한다. **최적 정확도: 10분 미만 영상.**

### 자막·전사 우선순위

1. 플랫폼 네이티브 캡션 (yt-dlp 자동 추출, 무료)
2. Groq Whisper API (fallback, 권장)
3. OpenAI Whisper API (fallback)

API 키는 `~/.config/watch/.env`에 저장하고 파일 권한을 `600`으로 제한한다. 설정은 멱등(idempotent)하여 재실행해도 안전하다.

### 주요 옵션

| 플래그 | 설명 |
|--------|------|
| `--start` / `--end` | 분석할 구간 지정 (예: `2:15`) |
| `--max-frames N` | 프레임 수 상한 지정 (토큰 절약) |
| `--resolution W` | 프레임 너비 (기본 512px, 텍스트 가독성 필요 시 1024) |
| `--whisper groq\|openai` | 전사 백엔드 강제 지정 |
| `--no-whisper` | 전사 비활성화 |

### 보안 모델

처리는 로컬에서 수행되며, Whisper API 사용 시 추출된 오디오만 전송한다 (영상 자체는 전송하지 않는다). API 키는 각 서비스에서만 사용되고 로컬 설정 파일에만 저장된다.

### 의존성

- `yt-dlp` — 동영상 다운로드
- `ffmpeg` / `ffprobe` — 프레임 추출
- Groq 또는 OpenAI API 키 — 캡션 없는 영상 전사 시 필요

첫 실행 시 macOS는 brew, Linux는 패키지 매니저로 자동 설치 시도한다.

### 한계

- 100프레임 하드 캡, 최소 2 fps
- Whisper 업로드 한도 25 MB (~50분 오디오)
- 인증이 필요한 비공개 URL 미지원
- Claude Code에서 "코드 실행 및 파일 생성" 권한 활성화 필요

### 사용 예시

```
/watch https://youtu.be/URL 30초 지점에 무슨 일이 일어나나요?
/watch video.mp4 --start 2:15 --end 2:45 이 구간을 요약해줘
/watch ~/recording.mov UI가 언제 깨지나요?
/watch slides.mp4 --resolution 1024 이 슬라이드의 텍스트를 읽어줘
/watch lecture.mp4 --max-frames 50 --no-whisper 핵심만 요약해줘
```

---

## 코드 작성 원칙 (Karpathy Guidelines)

LLM의 흔한 코딩 실수를 줄이기 위한 행동 지침. 속도보다 신중함을 우선하므로, 사소한 작업에는 판단해서 적용한다.

### 1. 코딩 전에 먼저 생각하라

가정(assumption)을 숨기지 말고 드러낸다.

- 가정을 명시적으로 밝힌다. 불확실하면 묻는다.
- 해석이 여러 가지라면 제시하고 선택한다 — 조용히 하나를 택하지 않는다.
- 더 단순한 접근이 있으면 먼저 말하고 반론한다.
- 혼란스러운 게 있으면 멈추고 무엇이 불명확한지 명시한다.

### 2. 단순함 우선

문제를 해결하는 최소한의 코드만 작성한다.

- 요청된 것만 구현한다. 투기적(speculative) 기능은 추가하지 않는다.
- 단일 용도 코드에 추상화를 도입하지 않는다.
- 요청하지 않은 유연성·설정 가능성을 넣지 않는다.
- 불가능한 시나리오에 대한 에러 처리는 쓰지 않는다.
- "시니어 엔지니어가 과하다고 할 것 같다" → 다시 쓴다.

### 3. 외과적 변경

필요한 부분만 건드린다. 내가 만든 것만 정리한다.

- 인접 코드·주석·포맷은 손대지 않는다.
- 동작하는 코드는 리팩터링하지 않는다.
- 기존 스타일과 맞춘다. 내 취향이 달라도 따른다.
- 무관한 데드코드를 발견하면 언급은 하되 삭제하지 않는다.
- 내 변경으로 생긴 고아(orphan) import·변수·함수는 직접 정리한다.
- 모든 변경 줄은 사용자 요청으로 직접 추적 가능해야 한다.

### 4. 목표 기반 실행

성공 기준을 정의하고 검증될 때까지 반복한다.

- 태스크를 검증 가능한 목표로 변환한다:
  - "검증 추가" → "잘못된 입력 테스트를 작성하고 통과시킨다"
  - "버그 수정" → "버그를 재현하는 테스트를 작성하고 통과시킨다"
  - "X 리팩터링" → "변경 전후로 테스트가 모두 통과함을 확인한다"
- 여러 단계 태스크는 단계와 검증 체크포인트를 포함한 계획을 먼저 제시한다.
- 약한 기준("작동하게 해줘")은 즉시 구체화를 요청한다.

---

## 일반 규칙

- 주석은 기본적으로 작성하지 않는다. WHY가 코드에서 명확하지 않을 때만 한 줄 작성한다.
- 보안 경계(사용자 입력, 외부 API)에서만 유효성 검사를 한다.
- UI 변경 시 개발 서버를 실행해 브라우저에서 직접 확인한다.
- PR 제출 전: 중복 PR 확인, 실제 문제인지 검증, 변경이 core에 속하는지 판단한다.

## 브랜치 전략

- 기능 개발: `feature/<name>`
- 버그 수정: `fix/<name>`
- 기본 개발 브랜치: `main`
